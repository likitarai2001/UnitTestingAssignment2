server:
  port: 9090

spring:
  datasource:
    url: jdbc:h2:mem:social_media_db;NON_KEYWORDS=USER
  h2:
    console:
      enabled: true
      path: /h2_console
  data:
    jpa:
      repositories:
        bootstrap-mode: default
  jpa:
    defer-datasource-initialization: true
    show-sql: true
